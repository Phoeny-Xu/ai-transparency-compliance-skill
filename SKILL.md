---
name: ai-transparency-compliance
description: |
  生成中国大陆、欧盟、美国加州三地 AI 生成内容透明度/标识合规义务清单或对比报告的方法论与护栏。
  模式A 产品合规评估（问卷→角色判定→效力核验→义务清单）；模式B 法规梳理研究（跳过问卷，直接出单法域/双边/三边/专题对比）。
  法规基础：中国《互联网信息服务深度合成管理规定》《生成式人工智能服务管理暂行办法》《人工智能生成合成内容标识办法》及 GB 45438-2025；欧盟 AI Act 第50条及配套透明度行为准则；加州 CAITA（SB 942 经 AB 853 修正）与 SB 1000。
  触发词：AI透明度、AIGC标识、生成合成内容标识、深度合成标识、AI Act透明度、SB 942、AB 853、SB 1000、出海合规、三地对比、中欧对比、法规梳理。
  法规时效状态见 SKILL.md「维护信息」节；免责声明见 README。
agent_created: true
---

# AI透明度三地合规查询（中国大陆/欧盟/加州）

## 0. 执行地图（10 秒扫完，先读这里）

**模式A · 产品合规评估（四阶段＋交付门禁）**

| 步 | 动作 | 读什么 | 产出 | 门禁 |
|---|---|---|---|---|
| 1 | 信息采集 | `assets/modeA-questionnaire-business.md`（**默认皮肤**）或 `assets/modeA-questionnaire.md`（法务版：判定母版＋opt-in 呈现皮肤；分流见阶段1），均含开场声明与回显规则 | 画像＋客观事实＋`survey_audit.json` | 回填确认、题间矛盾即时弹窗（`scripts/resolve_triggers.py` 机器复核） |
| 2 | 角色/法域判定 | `references/role-mapping.md` | 环节定位＋三地法定角色 | 双锚定、展示推理依据（`scripts/lookups.py` 机械判定项） |
| 3 | 效力核验 | `references/validity-checklist.md`（条件触发） | 效力核验记录 | 来源失效降级 |
| 4 | 生成报告 | `assets/modeA-report-template.md` ＋ `references/reporting-rules.md`（骨架可由 `scripts/build_skeleton.py` 生成） | 《三地AI透明度合规义务清单》 | 报告硬性要求 |
| 5 | 交付前门禁 | 本文件 §6 自检清单（`scripts/check_report.py` ＋ `scripts/lint_terms.py`；范围含欧盟时另跑 `scripts/cop_digest_verify.py` ＋ `scripts/validate_cop_coverage.py`） | 通过→交付 / 不通过→返工 | 全部勾选方可交付 |

**模式B · 法规梳理研究（四步＋交付门禁）**：定输出形态 → 读对应规则库（三边对比另读 `role-mapping.md`）→ 效力核验（同模式A）→ 按模板生成（`modeB-research-review-template.md` / `modeB-comparison-template.md`）→ §6 门禁。

## 1. 底线（6 条，违反即任务失败）

- **L1 核实原文后方可输出**（管内容对错）：每一项义务、观点以官方文本为依据；引用法条前先读 `references/` 规则库，未覆盖或标「待核原文」的先联网核实，不得凭记忆或二手资料写确定性表述。**脚注引文一律逐字取自原文块**（规则库「××原文」引用块／cop-digest 锚点回读的 sources 原文／联网核验的官方原文）；规则库无原文块的条文，**先补原文块或标「待核原文」再引用**，不得凭记忆直接写引文。cop-digest（`references/cop-digest.md`）为二级摘要，用于定位与框架；逐字引用条文、关键数值/日期、报告脚注一律按其锚点回原文，不得以 digest 替代官方文本。
- **L2 客观事实不推定**：能力/功能存在性、规模量级、是否已实施等客观事实，用户未确认或答「不确定」时一律条件式「若X成立则触发Y」，不得推定为成立；「最严口径」仅用于判定「义务是否触发」，绝不用于推定客观事实成立。
- **L3 义务措辞与术语**：强制性「应当」、非强制「建议」/「可以」；中文术语译法以 `references/glossary.md` 为准，禁用工程音译（「鲁棒性」→「稳健性」）。
- **L4 引用可核验**（管引用形式，区别于 L1 管内容对错）：正文以中文转述义务（可读性优先）、以条号援引；脚注引官方原文＋条号（欧盟/加州英文、中国中文），正文对应条号处有上标锚点，无孤儿脚注。
- **L5 排版三分离**：生成指令不入交付物正文；判断/推理/假设归「批注」节（docx 转 Word 批注）；正文去个性化（无问卷编号 A×/B×、环节编号、内部流程术语）。**引用块边界**：仅报头声明区（主标题下、首个二级标题前）可用 `>`，正文（首个二级标题起）一律禁用 `>`。
- **L6 效力核验记录必备**：报告必含「效力核验记录」节，每部被引法规一行「效力状态／核验日期／来源」。

## 2. 模式路由（接到请求先定模式）

**唯一判据**：用户要「评估具体产品/业务」，还是「梳理/对比法规本身」。

- **模式A**：点明具体产品/业务形态或角色，且要「合规义务清单」→ 走四阶段＋交付门禁（§0 地图）。
- **模式B**：动作是「对比/梳理/研究」，对象是法规制度本身 → 跳过问卷与角色判定，走 §4。

**判定要点**：
- 产品名、量级、模态等细节没给，是启动问卷的理由，**绝不是跳过问卷进模式B的理由**。
- 判定后报告开头加可逆提示（「本报告按产品合规评估生成，如需法规梳理请告知」；模式B同理反向提示）。
- **两可地带（★先问、禁止自行判定）**：如「上游大模型API提供商在中欧加三地部署」这类「角色/业务类型＋法域范围」但无产品细节，或无法一眼断定意图 → 照搬以下固定问法后再定模式：

> 请问您是要——
> ① 针对您的具体产品/业务，一步步问清产品形态、功能、服务对象后，给出「贵公司该做的」具体合规义务清单（会先做一轮问卷，目的是为您的产品/业务提供【精准】合规指引）；
> ② 不针对具体产品/业务，按法规全方位梳理某角色/某法域的【一般性】合规义务（不做问卷，直接出法规梳理/对比报告，该报告【无法】为某产品/业务提供精准合规指引）？

## 3. 模式A · 四阶段

### 阶段1：信息采集
**目标**：采集画像与客观事实，供角色判定使用。

**起始分流（★发出任何问卷前必问）**：先用一句固定话术判断走哪套问卷皮肤——按"偏好哪种题干风格"分流，而非按岗位，也不要求答题人自评法律水平（两套皮肤问的是同一批产品事实，差别只在题干用语是合规口径还是日常白话；法律角色与法域判定一律由 agent 在阶段2完成）：

> 您偏好什么风格的问卷类型，精炼还是通俗易懂？
> ① 我喜欢题干描述精炼（示例：您的产品在该地区是主动提供，还是被动可达？）
> ② 我喜欢题干描述通俗易懂（示例：您的产品在该地区有没有主动做营销、运营或提供服务，还是用户自己找过来用？）

（以上话术块**整块为用户可见原文**：问题、两个选项及其括号内示例共三部分必须一并呈现，不得只发问句、不得剥离或改写示例——示例是答题人判断「精炼／通俗」的唯一依据；文件路径、皮肤命名等 agent 侧信息一律留在话术块外，不随话术发出。）

- 选① → 走 `assets/modeA-questionnaire.md`（法务版：判定母版；呈现层为 opt-in 合规框架皮肤），其余流程不变。
- 选② → 走 `assets/modeA-questionnaire-business.md`（**默认皮肤**；问法见该文件，答案落盘时按"业务题 → 现有键名"映射表转换）。**该文件已部署（2026-09-17 批次二），皮肤选定后按其红线执行，无回退**。
- 未明确（含答非所问）时默认②业务版——分流问题发出后用户答非所问的，用一句白话重述选项再问一次；仍不明确的按②进行，并在下一条回复中告知「本次按通俗易懂的问卷进行，如您偏好精炼表述可随时切换」。

1. 照搬【所选皮肤文件】「问卷开场」节的提问前声明与范围声明，先于问卷发出（法务版→`assets/modeA-questionnaire.md`；业务版→`assets/modeA-questionnaire-business.md` §二，两版开场为镜像关系）。
2. 先发 A 组（按 A6→A3→A1→A2→A4→A7 顺序呈现，编号不变；A4 分法域逐轮采集，其余一轮问完），再按答案条件触发 B 组（勾选清单，单选标「（单选）」、多选标「（多选）」，每题附「其它（请补充）___」与「不确定（按最严口径）」）；B 组在 A 组全部完成后统一评估触发，A4 分轮期间不提前触发。
2.5 **题面锁铁律（★强制）**：严格按【所选皮肤文件】的题号顺序呈现「问题」列中面向用户的部分（题干、选项及括号内示例）——法务版→`assets/modeA-questionnaire.md`，业务版→`assets/modeA-questionnaire-business.md`；并遵守：
   - 题干不得改写语义层级——事实题不得包装成「是否构成X」等法律判断题；
   - 题内逻辑关系严格保持——OR 不得改 AND、AND 不得改 OR、多选不得改单选、单选不得改多选；
   - 选项列表不得增删——封闭列举不得额外加项、既存选项不得删除、题后「如微博、X」类示例必须照搬；
   - 判定备注里的解释性内容不得混入选项——判定口径归 `references/role-mapping.md`，不属于用户答题项；
   - 如问卷原题未覆盖某一画像细节，追加独立追问到原题之后（显式标注「画像补充题，原 modeA-questionnaire 中无对应题号」，格式见下方追问模板），不得改造、替换、压缩原题；
   - 允许的改写以 modeA-questionnaire.md 顶部「呈现白名单」五类为限（槽位填充、回显代替重问、同轮合并排版、触发原因标注、内部指令剥离），白名单之外的一切改写均属题面变形。
   - **换肤规则（★已生效，业务版 2026-09-17 部署）**：皮肤选定后按对应皮肤红线执行。法务版仍适用上方五类白名单；业务版皮肤在五类基础上扩展两类——「法律角色降白话」（仅把法律角色与法律判定层降为大白话，技术锚点词保留）与「保留法定／技术锚点词并附白话括注」（括注不得改变语义、不得替代锚点词）。业务版文件随其全文审毕后部署至 `assets/modeA-questionnaire-business.md`；未部署前不启用该皮肤，本句暂不生效。
2.6 **发出前自检（★强制，问卷层门禁）**：每轮问卷发出前，按 `references/ui-landing-rules.md` **§6** 两张自检表逐项核对——**⑦b** 高风险锁定题（A4／B1③／B4②(a)／B6／B9）题干要件与选项集逐字完整、示例与补充行随主问句同渲染；**⑤** 多选题（A3／A6／B1①／B1②／B5a）选项逐项列出、未改成单选、未按最严口径把未选项计为已选。发现缺失的**当轮补问**并记入审计记录。二者在**报告端不可机器化**（报告只见结论、不含当轮题面证据），故前移至此；报告端 §6 ⑤／⑦b 仍保留人工终判。
2.7 **规模问题压缩规则（★强制）**：A4(a)先采三地摘要，统计对象、时间窗口、去重方法、证据来源合并为一张口径卡；已由用户提供的数字直接回填，不逐法域重复确认。B3a优先复用A4(a)同一产品、同一统计对象和同一口径，只有口径不同、接近门槛或不确定时补采差异项；B4②(c)仅在平台角色候选成立时一次性采集此前12个月数据及接收用户/创作者用户口径。不得把B3a与B4②(c)合并为一个总月活问题，也不得为确认同一数字反复发起自然语言追问。
3. 每轮回复固定回显四要素（按所选皮肤取原文：法务版→`modeA-questionnaire.md`「问卷开场」节；业务版→`modeA-questionnaire-business.md` §2.3 镜像节）。
4. 完成问卷后、进入阶段2前，先回显画像给用户确认（强制，不可跳过）。

**欧盟CoP签署事实（B11）**：A6含欧盟时必须采集 `B11_cop`（已签署／计划签署／未签署／不确定＋涉及Section）。这只是客观事实，不让用户判断provider/deployer；状态不确定不得推定已签署。阶段2仍由agent独立判断相关Section。

**画像补充追问模板**：发现原题未覆盖的画像细节时，按「{题号}-增{n}（画像补充题，原 modeA-questionnaire 无对应题号）：基于您上述描述，追加确认：{一句话、可作答}」格式追加（编号如 A4-增1、B4-增2）；增问答案与正式题答案同等进入画像与审计记录。

> **本阶段门禁（细则见 modeA-questionnaire.md）**：回填确认区分「事实采集结果列／agent 推导的法律结论列」，用户只确认事实、不答法律判断题；答案历史可追溯（旧答案＋更正时间＋原因入审计记录，不覆盖不静默替换）；A3×B5a、B4②平台类型×量级等答案级矛盾即时弹窗澄清；B3a与B4②仅在统计对象、地域、周期、去重方法及用户集合均一致时才可比较，否则记录为统计口径提示，不作硬性矛盾，不延后到回填环节自行推断。

### 阶段2：角色与法域判定
**目标**：定位价值链环节并映射三地法定角色。
读取 `references/role-mapping.md` 执行三步判定；**判定前先建「单一活动→角色图」**（按「模型/系统控制→产品交付→内容分发→下游发布」排序，每项活动只落一次角色），共享工程/流程控制只列一次并链到所有受影响活动与法域。

**本阶段门禁（细则见 role-mapping.md）**：
- 法域适用性前置：按 A4 判定各法域是否适用（中国「向境内公众提供」／欧盟投放市场或投入使用／加州「公开可访问」，含境内生成/境外发布分层框架）。
- 角色判定双锚定：同时结合「定义条款＋义务条款」综合判断，不得仅凭义务条款倒推或仅凭定义条款孤立定性；推理依据写明「依据定义条款X＋义务条款Y」。
- 判定过程必须向用户展示推理（哪个画像事实→哪个环节→哪个角色），不得黑箱出结论。
- A7=是（重大影响场景）时按「高风险预警分支」在报告头部加醒目警告。

### 阶段3：效力核验（条件触发）
**目标**：确认被引法规现行有效并留下可追溯记录。
规则库即已核实原文（bundled=已核实），默认直接引用、不逐条现搜。**仅当**以下任一条件满足时读 `references/validity-checklist.md` 联网核验：①last_verified 超90天；②用户要求复核；③引用超出规则库覆盖；④触及「监控点」。

**来源失效降级（★）**：官方来源不可达时不得静默用旧文本顶替，须记录「失败来源＋最后核验日期＋待确认事实＋责任人＋是否阻断上线」，仅以条件式/历史性表述继续（如「截至{最后核验日}现行有效；本次未能联网复核，请以官方最新文本为准」）。

无论是否现搜，报告必含「效力核验记录」节（见 L6）。

**全称与简称系同一份文件（★）**：同一部法规的正式全称与通行简称是同一份文件（如《互联网信息服务深度合成管理规定》与「深度合成规定」、《生成式人工智能服务管理暂行办法》与「暂行办法」），效力核验**只做一次、效力表只占一行**，不得因正文出现两种写法而重复核验或重复列行。

### 阶段4：生成报告
**目标**：按模板产出义务清单。
读取 `assets/modeA-report-template.md` 生成《三地AI透明度合规义务清单》。生成前询问两件事：①报告语言（纯中文/中英双语）；②是否脚注注明法条原文（用户未表态默认「纯中文＋正文以条号援引」，不因偏好缺失阻断流程；用户主动要求再切换）。

**报告硬性要求（摘要，细则见 `references/reporting-rules.md`）**：
1. 义务按「价值链环节×法域」组织，同一环节三地并排；每项含义务内容、法条锚点、生效/适用日期、责任主体、未合规后果。
2. 附三地罚则对比；角色判定结论附推理依据（双锚定）。
3. 义务区分「已确定/待核实（条件式）」，第一节列「待核事实清单」；每项标注证据等级（法条原文/静态整理/规则推演/实测）。
4. 法规要求与落地建议严格分列（建议部分标「建议措施」）。
5. 中国法标识义务按「总体要求→显式标识→隐式标识」组织，关联条文合并同项。
6. CoP 措施按「义务类型（Commitment分组）」分点展开。
7. 落地建议按「义务主题」跨法域综合论述。
8. 正文去个性化（见 L5 与 reporting-rules §三）。

其余格式细则（双语段段对应、正文洁净、脚注格式、法定术语与释义）见 `references/reporting-rules.md`。

**机器预检（★交付前必跑）**：报告 Markdown 定稿后、转 docx 前，跑一遍

```bash
python scripts/check_report.py <报告>.md     # 结构/引用/去个性化/法域门禁（E/W 两级）
python scripts/lint_terms.py   <报告>.md     # 术语与禁用词（对标 glossary.md）
python scripts/cop_digest_verify.py          # 仅范围含欧盟时：CoP digest 完整性/锚点校验
python scripts/validate_cop_coverage.py <矩阵>.json <报告>.md --audit <survey_audit.json>  # 模式A且含欧盟；modeB省略--audit
```

两级发现：**E（error，硬阻断，退出码 1）须全部清零**；**W（warning，人工裁定，退出码 2）逐条确认或豁免**。确因合理情形须保留的，在该行加豁免注释 `<!-- lint:ignore E-03 -->`（文件级 `<!-- lint:ignore-file W-08 -- 理由 -->`），豁免会在末尾 `W-00` 汇总留痕。**linter 只报警不自动改**——自动改写会破坏法条原文与脚注锚点，违反 L1。

`cop_digest_verify.py` 校验 CoP digest 的源指纹、25项完整性、锚点与固定注册表；`validate_cop_coverage.py` 校验个案相关Section是否逐项分类、适用/条件适用的will是否进入报告、S1 M1.1/M1.2/M3.4关键点是否展开、全部已触发义务是否映射落地建议。**范围含欧盟时两者均为交付前必跑**；返回1须先修正，返回2须人工查明后再放行。个案矩阵只承载分析结论，不由脚本替代法律判断。

**交付前必过 §6 交付前门禁，全部通过方可交付。**

## 4. 模式B · 流程

1. 确认输出形态：单法域梳理 / 双边对比 / 三边对比 / 专题对比。法域组合由用户指定。
2. 读取对应法域规则库（三边对比另读 `role-mapping.md` 作骨架）。
3. 执行阶段3效力核验（条件触发规则同模式A），保留「效力核验记录」节。
4. 按模板生成：单法域用 `modeB-research-review-template.md`（按义务主体展开）；对比用 `modeB-comparison-template.md`（十二维框架）。
5. 生成前同样询问报告语言、是否引脚注原文（默认规则同模式A）。

**模式B硬性要求（摘要）**：法条原文与分析评论严格分节；正文（首个二级标题起）禁用灰色引用块（`>`），报头声明区除外；正文声明性内容用普通段落/表格；排版三分离同样适用（L5）；**含欧盟时须扫描 CoP 并就地输出其对应义务，报告不另设独立的「CoP 措施要点展开」章节**——对比报告在矩阵欧盟列标 CoP 编号＋层级标签（will/encouraged/may）＋措施要点摘要，并在第五节各主体项下逐条四要素详述；单法域梳理在义务清单设「行为准则对应（欧盟适用）」列，并在各欧盟主体下设第5小节「成文法义务与行为准则义务的衔接」逐条展开（通用法律地位在「二、义务主体与义务内容」开头总述）；另建 `cop_case_matrix/1`，无具体签署主体时状态记「不确定」且不推定签署；多法域对比设第七节「相同点/不同点/共通要求/单法域特别规定」。

交付前同样必过 §6。

## 5. 法域处理索引（条件触发，快速定位）

| 触发条件 | 必读 | 关键动作 |
|---|---|---|
| 范围含**加州** | `references/ca-rules.md` | 按 ca-rules.md「〇、效力状态开关」定义务基准；SB 1000 修订对照与呈现规则见 `references/sb1000-diff.md` |
| 范围含**欧盟** | `references/eu-rules.md` | 必读 `references/cop-digest.md` 的规则摘要＋固定注册表；建立个案矩阵逐项分类，逐字引用按锚点回原文（细则见 reporting-rules.md §五） |
| 范围含**中国** | `references/cn-rules.md` | 标识义务按「总体要求→显式→隐式」体系化组织（细则见 reporting-rules.md §四） |

## 6. 交付前门禁（自检清单，逐条勾选，全部通过方可交付）

> 每项一行；「不通过即拦截」。细则指向各文件，错误示范不入正文。
>
> **机器预检**：`python scripts/check_report.py <报告>.md` 与 `python scripts/lint_terms.py <报告>.md`；范围含欧盟时另跑 `python scripts/cop_digest_verify.py`（digest 源指纹与锚点校验）。下列各条后括注 `[E-xx]`＝该条的机器检查码（E 级须清零，W 级人工裁定）；未括注者＝**机器不可判，须人工勾选**（法律定性、推理质量一类）。
> 机器检查码总表见 `scripts/check_report.py` 的 `CATALOG`；用语自查见 `scripts/lint_terms.py`。
> **术语扫描范围（2026-09-15 定）**：W 级检查不扫**报头声明区**（范围声明／时效声明／依据等强制照搬或元数据行，与 E-04 共用同一条边界）与**批注节**；硬禁用词「鲁棒」`[E-20]` 全文生效。细则见 `references/reporting-rules.md` §三.4。
> **退出码约定**：linter 家族（check_report／lint_terms）`0=通过、1=有 E、2=仅 W`；引擎家族（resolve_triggers／build_skeleton）`0=通过、1=业务矛盾（须澄清后改答案）、2=未执行（输入缺失／结构不符／不变量违反）`。**引擎返回 2 时不得当作通过继续流程**。

**A. 引用与效力底线（L1/L4/L6）**
- [ ] ① 效力核验记录节存在，每部法规一行 `[E-01]`
- [ ] ①b 效力核验记录覆盖率：正文援引的每部法规在效力表各有一行 `[W-14]`（法规识别为启发式；背景性/排除性提及与来源 URL 内的 bill_id 不计入；**同一部法规的全称与简称合并计一行**）
- [ ] ①c 阶段3 触发条件已逐条判定并留痕：四个触发条件（last_verified>90天／用户要求／引用超覆盖／命中监控点）逐条给出结论与日期；命中监控点者记录核验结果——含**欧盟两监控点**（Art. 50 指南正式通过版是否发布；CoP 签署名单／准则修订／Measure 3.4 进展／**sources txt 的 MD5 是否与 digest 头部登记一致**）——人工
- [ ] ② 引用格式规范（正文引 glossary、涉原文引法条原文）——人工
- [ ] ③ 脚注为官方原文全文＋上标锚点，无孤儿脚注 `[E-05]`，无「含……」「等」概括性脚注 `[W-02]`；cop-digest 关键句为截取句，仅供内部定位与理解，不得直接作为报告脚注

**B. 事实客观性底线（L2）**
- [ ] ④ 客观事实不推定：不确定项以「若X则Y」＋「待核事实清单」呈现，无「推定为具备/达量/已实施」`[W-16]`（假设节/批注节豁免；「保守推定」「文本未直接界定」类自述口径不算；仅模式A）
- [ ] ⑤ 多选题（A3/A6/B1①/B1②/B5a）逐项列举，无「按最严口径全列」（仅用户答「不确定」的选项可按最严口径，并在假设节标注待核对）——人工（报告端只见结论、不含题面，故**前移为问卷层自检**：`references/ui-landing-rules.md` §6.1＋阶段1 第 2.6 项；本行保留人工终判）
- [ ] ⑥ 量级口径统一（月活 vs 累计）`[scripts/resolve_triggers.py 交叉校验]`；**能力×模态相容**：B5a 勾选的高仿真能力所需输出模态须在 A3 中存在（如纯文本产品不得勾「数字人／人脸生成／沉浸式拟真场景／合成人声」）`[同上，cross_check_capability_modality]`
- [ ] ⑦ 回填确认记录客观（含确认/更正内容，无空洞断言）——人工；**条目存在性**已可机检 `[W-19]`（模式A 批注节应含「回填确认记录」条目；批注节整体缺失时亦报，防静默跳过）
- [ ] ⑦b 问卷题面完整性抽查：高风险锁定题（A4/B1③/B4②(a)/B6/B9）的题干要件与选项集已完整呈现，无增删、无逻辑关系改写、无示例省略（⑦b 为增量项、不参与重编号）；发现缺失的，当轮补问并记入审计记录——人工（**前移为问卷层自检**：`references/ui-landing-rules.md` §6.2＋阶段1 第 2.6 项；报告不含题面证据，报告端仅人工终判）

**C. 报告结构底线**
- [ ] ⑧ 范围声明原样照搬 `[E-02]`；未被套话改写 `[W-11]`
- [ ] ⑨ 未覆盖维度按产品列出，仅引 out-of-scope-pointer.md 收录内容 `[W-17]`（未收录者须写「需另行专项评估」；段缺失或段内无条目均报；仅模式A）
- [ ] ⑩ 同法域多角色分小节＋「角色间义务关系说明」（承载落点：`assets/modeA-report-template.md` §三【指令】＋三法域节末占位；骨架 `scripts/build_skeleton.py` 第三节各法域节末占位；规则出处 `references/role-mapping.md`「多环节叠加规则」）——人工
- [ ] ⑩a 义务详述节每项含核心字段（责任主体／生效适用／未合规后果）`[E-10]`
- [ ] ⑩b 义务详述每项标「证据等级」（法条原文引用／规则库静态整理／规则推演／实测结果）`[E-11]`
- [ ] ⑩c 模式A 报告含「义务详述」节 `[W-15]`（节整体缺失时 ⑩a／⑩b 与 ①b 会静默跳过）

**D. 法域专属（仅查涉及法域）**
〔中国〕
- [ ] ⑪ 算法备案/安评标「法定触发状态」，正文无「B8」编号 `[E-03]`
- [ ] ⑫ 标识义务按「总体要求→显式→隐式」，关联条文合并同项 `[E-07]`（核验动词句不算标识义务句；角色仅分发/传播平台时降级 `[W-13]` 复核主体定性）
- [ ] ⑬ 〔非透明度义务〕条目详述节标题加后缀 `[W-10]`
- [ ] 中国法禁用词（「鲁棒」等）清零 `[E-06]`／术语误译清零 `[lint_terms W-20]`（扫描范围＝正文＋要点概览，报头声明区与批注节豁免）
〔欧盟〕
- [ ] ⑭ CoP **就地展开、不另设独立章节**（对比报告落位矩阵欧盟列＋第五节各主体项下；单法域梳理落位各主体第5小节；模式A落位分法域义务详述欧盟节），按 Commitment 分组分点、**组内逐条四要素展开（要求内容/技术操作要点/层级标签/来源，缺一不可）**，关键要点不得省略（M1.1 元数据+水印两层、M1.2 不可移除、M2.1 免费提供、M3.4 互操作 2027-02-02 四选一路径）`[W-12 未分组的弱信号]`，已读 cop-digest（逐字引用处按锚点回原文）、不得只搬速查表；触发 CoP 却未见任何措施展开 `[E-08]`
- [ ] ⑭b 范围含欧盟时已跑 `cop_digest_verify.py` 且 **0E/0W**（digest 源指纹 MD5／行数字节数／Measure 数 S1=17、S2=6+2／锚点区间未失效；**出现 W 亦须查明再放行**——C-03「未登记锚点」曾据此检出 S2 分组标题命名漂移）`[scripts/cop_digest_verify.py]`
- [ ] ⑭c 范围含欧盟时已建立 `cop_case_matrix/1`：相关Section固定注册表**全部逐项分类**，适用/条件适用的will均有正文覆盖标记，S1-C1-M1.1／S1-C1-M1.2／S1-C3-M3.4 required_points无缺漏，全部适用CoP项及 `triggered_obligations` 已映射落地建议；已跑 `validate_cop_coverage.py` 且0E/0W。模式A另以 `--audit` 核对B11签署事实 `[scripts/validate_cop_coverage.py]`
- [ ] 欧盟共用语言/自用豁免滥用（own-use black-list）弱信号 `[W-09]`
〔加州〕
- [ ] ⑮ SB 1000：联网核验签署状态并按「效力状态开关」确定义务基准；效力核验表含 SB 1000 状态行 `[E-09]`；报告头部时效声明含 SB 1000 状态说明；待签署期修订点集中「前瞻变更提示」块、不散落正文挂角标；范围未含加州却出现 SB 1000/leginfo `[W-04]`；罚则表混入「前瞻」表述 `[W-05]`
- [ ] ⑯ 三类状态区分（当前不触发/已适用待生效/若适用）不混框 `[W-07]`
- [ ] ⑰ 多原因不触发分层（主体门槛主导＋层级抗辩），不括号并列 `[W-07]`
- [ ] ⑱ 「已适用待生效」与「主体资格未达」分列；SB 1000 前瞻提示仅附「主体资格未达」类 `[W-18]`（含「前瞻变更提示」「前瞻提示」字样的块不得挂于「已适用／义务待生效」类小节）
- [ ] 加州「不适用」行占比过高（>0.6）弱信号 `[W-06]`

**E. 正文洁净与格式（L5）**
- [ ] ⑲ 正文洁净：**正文（首个二级标题起）禁 `>` 引用块，报头声明区允许** `[E-04]`、去个性化（无问卷编号 A×/B×、环节编号、内部流程术语）`[E-03]`、排版三分离（docx 已转 Word 批注且正文移除该节）
- [ ] ⑳ 中英双语段段对应：叙事 1:1、标题/表格/元数据字段值双语，无「仅译首句、表格无英文」缺陷 `[W-01/W-08]`（细则见 reporting-rules.md §七）
- [ ] 措辞「必须」清零（强制用「应当」）`[W-03]`

**F. 模式专项（仅模式B）**
- [ ] ㉑ modeB：正文无灰色引用块（报头声明区除外）`[E-04]`；欧盟单法域梳理含各主体第5小节「成文法义务与行为准则义务的衔接」`[E-12]`（范围含欧盟时）；对比报告含第七节「相同点、不同点、共通要求与单法域特别规定」`[E-12]`；对比报告含欧盟时须见 CoP 展开（矩阵欧盟列编号＋层级标签＋摘要，第五节各主体项下四要素详述；**不另设独立章节**）`[E-08／W-12]`，CoP＋层级标签展开质量仍须人工复核（模板识别为启发式：以同模板他节标题为信号，防用待检节自身做信号）
- [ ] ㉒ modeB 批注语义**不串模式**（2026-09-20）：modeB 跳过问卷、**不针对具体产品**，故其批注须以「**法定构成要件（定义条款门槛／服务性质）→义务条款**」推演主体认定、「**法规效力／解释／范围层面**」为不确定项（时效未定、条文适用范围存解释空间、编排口径）；出现 modeA 专有语汇（画像事实／画像推断／用户答「不确定」）即报 `[W-25]`——该处原无任何机检码覆盖（W-19 仅管模式A 的「回填确认记录」条目），两套 modeB 模板的批注占位句曾长期沿用 modeA 语汇而「双绿」不暴露。**两条边界**：门控＝模板特征标题命中（同 W-24，防对「关于 skill 的」工作稿误报）；**不收「回填确认」**（属模式A ⑦、modeA 报告中系法定必填，正文滥用另由 E-03 治理）

## 7. 文件索引

**命名约定（看名知模式）**：`assets/` 模板文件名带模式前缀——`modeA-` 仅模式A（产品合规评估）使用、`modeB-` 仅模式B（法规梳理研究）使用；两模式皆用者不加前缀（当前四个模板均为模式专属）。`references/` 与 `scripts/` 为两模式共用，一律无前缀。

| 文件 | 内容 | 使用时机 |
|------|------|----------|
| `references/cn-rules.md` | 深度合成规定、生成式AI暂行办法、标识办法、GB 45438-2025 | 涉及中国必读 |
| `references/eu-rules.md` | AI Act Art. 50/99(4)(g)、Digital Omnibus、CoP、Art. 50 指南、GPAI指南 | 涉及欧盟必读 |
| `references/cop-digest.md` | CoP 全量摘要（25单元）＋固定个案适用与覆盖注册表；各条附层级标签与原文锚点 | 含欧盟时必读；逐字引用按锚点回原文，个案矩阵按注册表逐项分类 |
| `references/ca-rules.md` | B&P Code §§22757–22757.6（SB 942 经 AB 853 修正）＋ 效力状态开关（SB 1000 义务基准切换） | 涉及加州必读 |
| `references/role-mapping.md` | 价值链七环节×三地角色对应表、双锚定规则、横向差异六规则、法域适用性前置 | 阶段2必读 |
| `references/validity-checklist.md` | 每部法规效力核验检索点 | 阶段3触发时 |
| `references/glossary.md` | 中英文术语对照表（译法唯一依据，L3） | 正文转述必查 |
| `references/reporting-rules.md` | 报告硬性要求细则（体系化/CoP分点/双语/去个性化/法定术语） | 阶段4必读 |
| `references/sb1000-diff.md` | SB 1000 确定性修订对照与呈现规则 | 涉及加州修订点 |
| `references/out-of-scope-pointer.md` | 未覆盖维度清单 | 报告「未覆盖维度」节 |
| `references/sources/` | 官方原件（PDF 用 pypdf 提取，Windows 设 PYTHONUTF8=1） | 需核对原文时 |
| `references/survey-audit-schema.md` | survey_audit.json 结构化答案契约（回填确认的机器可读层） | 阶段1回填确认 |
| `references/ui-landing-rules.md` | 问卷前端／工具落地注意事项（内部落盘逻辑不进用户可见层；范围／说明行随主问句同渲染；分流问问题·选项·示例三部分整体呈现；呈现锁题号索引；**§6 发出前自检＝⑤多选逐项列举／⑦b 高风险锁定题题面完整性的问卷层门禁**） | 阶段1呈现问卷时（含**每轮发出前**跑 §6 自检） |
| `assets/modeA-report-template.md` | 模式A 义务清单模板 | 阶段4 |
| `assets/modeB-research-review-template.md` | 模式B 单法域梳理模板 | 模式B |
| `assets/modeB-comparison-template.md` | 模式B 对比报告模板（十二维） | 模式B |
| `assets/modeA-questionnaire.md` | **判定母版（唯一权威）**＋opt-in 合规框架呈现皮肤（仅答题人选①时以其为题面与开场来源）：A/B 组问卷＋六类标签（五类改写白名单＋高风险锁定；题内标签含【呈现】【询问】【判定】【校验】【落盘映射】）＋呈现白名单＋开场声明＋回显四要素＋回填确认规则 | 阶段1 |
| `assets/modeA-questionnaire-business.md` | 模式A 业务版问卷（**默认皮肤**：只换问法、判定规则层全继承法务版；含分流话术、E组入口题、值域归一映射、附录A速查表） | 阶段1 |
| `assets/cop-case-matrix-template.json` | CoP个案矩阵空模板（`cop_case_matrix/1`） | 范围含欧盟时复制到报告同目录 `.audit/` 后逐项填写 |
| `scripts/check_report.py` | 报告结构/引用/去个性化/法域门禁 linter（§6 机器预检，E/W 两级，退出码 0/1/2） | 交付前必跑 |
| `scripts/lint_terms.py` | 术语与禁用词 linter（对标 glossary.md；W 级不扫报头声明区与批注节） | 交付前必跑 |
| `scripts/survey_io.py` | `survey_audit.json` 统一读取（嵌套/扁平两种结构、`utf-8-sig` 容 BOM、结构校验硬失败） | 阶段1、阶段4共用 |
| `scripts/resolve_triggers.py` | B 组触发引擎＋答案级校验（B3a、A4(a)、B4_2c各自值域与详情完整性；仅在统计口径完全一致时允许比较；能力×模态：B5a×A3单向防过度勾选；退出码0/1/2） | 阶段1回填确认 |
| `scripts/lookups.py` | 机械判定查表：中国场景映射／加州 large online platform 三关／SB 1000 基准 | 阶段2、阶段3 |
| `scripts/build_skeleton.py` | 由 survey_audit.json 生成报告骨架（只填行主键、不写定性） | 阶段4 |
| `scripts/cop_digest_verify.py` | cop-digest.md 完整性校验（源指纹 MD5／行数字节数、Measure 完整性、锚点区间与关键节点） | 维护期（CoP 改动后）＋**报告生成期（范围含欧盟时交付前必跑）** |
| `scripts/validate_cop_coverage.py` | CoP个案矩阵、will正文覆盖、关键点展开及已触发义务→落地建议映射校验 | 范围含欧盟时，Markdown定稿后、转docx前必跑 |
| `tests/register_case.py` | 盲测案例登记（追加索引行与逐案明细＋可选匿名 git 提交，不覆盖既有行）；属 `tests/` 个人维护区，不进运行目录与 zip | 维护期 |
| `scripts/annotations_to_docx_comments.py` | 批注节 → Word 批注气泡（**锚点表须与三套模板的章节名对齐**：modeA 报告／modeB 对比报告／modeB 单法域梳理；未命中降级挂末段并出 stderr 告警——**告警非零即当缺陷处理**） | docx 交付 |
| `scripts/md_to_docx.py` | Markdown → docx 转换（含脚注注入；链接 scheme 白名单，降级时退出码 2） | 生成 docx |

## 8. 维护信息

**当前版本：v1.1**（本地迭代，未对外发布；对外基线为 v1.0／2026-09-10 初版）

> **版本规则（2026-09-19 用户裁定）**：**不逐次升版**——日常修订（题干／备注／措辞／脚本修复／测试／文档）一律只进 git 提交、不动版本号；仅「①对外发布 ②全量重核法规 ③重大结构性改动」三种情形才升版本号并更新日期。下表「批次」列记录 **v1.1 名下累积的各批次**，不再各自编为独立版本（原 1.1→1.7 的逐次升号已并回 v1.1）。

| 批次（日期） | 变更摘要 |
| --- | --- |
| ⑮ 2026-09-20 | **规模问卷压缩呈现**：A4(a)改为三地摘要＋共享口径卡；B3a优先复用A4(a)同口径，仅在接近门槛、口径不同或不确定时补采；B4②(c)改为一次性提交此前12个月数据及平台用户口径，允许只填写与A4(a)/B3a的差异项；两套 questionnaire、UI落地规则与答案契约同步，题号和旧落盘字段不变。 |
| ⑭ 2026-09-20 | **CoP固定注册表与个案覆盖门禁**：不新建重复digest，在 `cop-digest.md` 内登记25个规则单元的主体、Art.50锚点、适用条件、签署效果、层级、required_points、模态、建议主题与原文锚点；新增B11客观采集签署状态/Section、`cop_case_matrix/1`模板及 `validate_cop_coverage.py`，强制相关Section逐项分类、适用will进正文、S1 M1.1/M1.2/M3.4展开、全部已触发义务映射落地建议。模板使用HTML侧车标记，转docx前机检、转换时剥离；法律判断仍由个案分析完成，脚本不预判产品。按版本规则不升版（仍v1.1）。 |
| ⑬ 2026-09-20 | **问卷数据契约与判定链修订**：`survey_io.py` 支持 `survey_audit/2` 并兼容迁移 v1，严格校验答案类型、枚举、未知键与 `history`，返回保留元数据的 `AuditBundle`；A3 拆分为功能与输出模态，纯检测/分析产品不再误触发生成类题；触发引擎补齐 A6/A3 规范化、中国连接点分层和独立的算法备案/安全评估状态；加州封闭列举平台缺失分发答案默认按定义行为处理、回答“否”改报矛盾；模式B不再套用模式A范围声明，Windows `check_report.py`/`lint_terms.py` 统一 UTF-8 输出；骨架补充不确定事实和 SB 1000 缺失状态提示。法律规则文本未改，测试目录仅留在 git 仓库本地并继续由同步脚本整目录排除。 |
| ⑫ 2026-09-20 | **modeB 批注语义去 modeA 化＋批注子节机检补位**（用户质疑触发：「modeB 不涉及具体产品，为什么会有画像事实和假设与不确定？」）。**其一（残留确认与改法）** 两套 modeB 模板的批注子节占位句长期沿用 modeA 语汇——`modeB-research-review-template.md` ①「画像事实→定义条款→义务条款」（原句抄自 `modeA-report-template.md` L44／L153）、②「用户答『不确定』或画像推断处」；`modeB-comparison-template.md` ②「企业角色判定」、③「用户未明确处按最严口径」。**改法为「改语义、非删项」**：modeB 有同构但不同源的内容——modeA 的留痕对象是**产品事实**，modeB 是**法条本身**。①改为「**法定构成要件（定义条款门槛／服务性质）→义务条款**」（主体为何落入／不落入某定义，依据全取法条）；②改为「**研究口径层面的不确定项**」（法规时效未定如 SB 1000 待签／条文适用范围存解释空间／编排口径／范围声明边界）；对比模板②改为「三地主体**概念对位**推理（功能映射，非法律同一性认定）」。子节名一律保留（不动锚点关键词）。**其二（机检补位）** 新增 **W-25**：模式B 报告出现 modeA 专有语汇（`画像事实`／`画像推断`／`用户答「不确定」`）即报——该处**此前无任何机检码覆盖**（W-19 仅管模式A 的「回填确认记录」条目存在性），故残留可长期存活且**「双绿」不会暴露**（判缺陷不得以双绿为依据）。扫描范围＝**全篇**（不设批注节边界——只扫批注节会在「正文写进去」时留新盲区）；**两条边界经语料裁定**：①用**精确短语**而非裸词「画像」（modeB 转介卡会合法提及 GDPR 的「画像」Art. 4(4)）；②**不收「回填确认」**（属模式A ⑦，modeA 报告中系法定必填；正文滥用另由 E-03 治理——首轮语料回归中 24 份文件 67 处命中全由此词贡献）；③门控＝**模板特征标题命中**（`is_comparison`／`is_research`，同 W-24），防对「关于 skill 的」工作稿与归档模板整片误报（模板未命中时 W-23 已出留痕）。仅模式B 生效，modeA 合法使用不报。**其三（既有缺口修复）** `annotations_to_docx_comments._find_anchor` 的锚点表按模板硬编码，单法域梳理模板批注 ①③ 原无映射、实测**降级挂文档末段**；按用户裁定两条均锚「**二、义务主体与义务内容**」，新规则置于末位（modeA／对比模板命中不受影响：规则按序尝试、不提前放弃）。**测试 205→213**（W-25 正例／反向验证／正文覆盖／modeA 边界／裸「画像」边界／不并「回填确认」／模板门控 7 例＋批注锚点 1 例；两处反向验证均通过——源码级整块摘除 W-25 后正例转红、锚点规则回退后新用例转红）。**语料回归（两轮；第二轮即收窄依据）**：首轮全量 `--mode B` diff 见 **24 份文件命中 W-25，全部为误报**（工作区多为「关于 skill 的」工作稿与归档模板，非 modeB 报告；命中量 67 处由「回填确认」一词贡献）——据此收窄为「**不收『回填确认』＋模板特征标题门控**」，第二轮同口径全量 diff **零差异**（`DIFF_EXIT=0`）。**法律口径零改动**；按版本规则**不升版本号（仍 v1.1）**；SKILL §6 新增 ㉒。 |
| ⑪ 2026-09-19 | **modeB 对比模板取消独立 CoP 章节，改为随欧盟义务就地展开**（用户裁定：「不需要单独一节《AI生成内容透明度行为准则》措施要点展开，而是改为在考虑欧盟法下义务时，必须扫描和详细输出 CoP 有关义务」）。**落位两处**——①对比矩阵欧盟列：CoP 编号（C× M×.×）＋层级标签（will＝应当／encouraged＝宜／may＝可）＋该维度措施要点摘要（与法定义务分句书写）；②第五节「企业合规义务梳理」各主体项下：表格之后按主体逐条详述相关 CoP 措施，**四要素缺一不可**（要求内容／技术操作要点／层级标签／来源）。无对应措施须显式写「无对应措施」并说明原因（如仅涉 Art. 50(1)/(3)，准则仅覆盖 Art. 50(2)、(4)、(5)），不得留白或虚构挂钩。**改动面** `assets/modeB-comparison-template.md`（头部指令＋矩阵与第五节两处落位指令＋四主体占位＋批注子节⑤说明）、`SKILL.md`（模式B硬性要求、§6 ⑭ 与 ㉑）、`references/reporting-rules.md` §五（新增落位规则第 0 条，标题改「就地扫描与分点论述」）。**顺带校正模板样例表的 CoP 编号错误**：检测措施属 Section 1 **C2 M2.x**（原样写 C1 M2.x）、互操作性 M3.4 属 **C3**（原并入 C1）、交互告知属 Art. 50(1) **CoP 无对应措施**（原样写 C2 M4.x）。**新增机检码**（独立节取消后暴露一处**静默失效**：对比报告无「欧盟」节标题 → 依赖 `structure["eu"]` 的原 E-08 在对比报告中永不触发，CoP 这一 ★强制项对对比报告实为无检）：`check_report.py` 增 **E-08 对比分支**（模式B 对比报告＋范围含欧盟＋内容涉 Art. 50(2)/(4)【相关性门 `COP_CONTEXT_KEYS`，防不涉 CoP 的专题对比误报】却全篇无 CoP 措施展开）＋新增 **W-24**（出现独立的 CoP 措施章节——反向锁死「不再单独成节」）；已展开判据放宽至 `M×.×`／`Measure ×.×`／`措施 ×.×` 三种记法，机检只抓「完全没扫 CoP」的硬缺口，展开深度仍归人工（㉑）。**批注锚点** 子节⑤由「八、行为准则措施要点展开」改锚「五、企业合规义务梳理」（单法域梳理模板保留「成文法义务与行为准则义务的衔接」为锚点词，两模板共用一条规则）。**测试 198→205**（E-08 对比分支 4 例／W-24 2 例／单法域梳理批注锚点 1 例；反向验证＝源码级整块摘除新码后正例转红、锚点回退旧词后对比用例转红）。**语料回归**：工作区 63 份 md 跑改前/改后 `--mode B` 全量 diff，仅 2 处新增且均为**真检出**（本次交付报告＋W-24 独立节；旧「中欧加州…规则异同」报告涉 CoP 但全篇无措施展开＋E-08），零误报；测试语料 13–18 全数实跑（0 跳过）维持原状。**法律口径零改动**；按版本规则**不升版本号（仍 v1.1）** |
| ⑩ 2026-09-19 | **批注锚点补 modeB 映射**（三地对比报告 docx 交付时暴露）。`annotations_to_docx_comments._find_anchor` 的 `title_rules` 原按 `modeA-report-template` 硬编码（四子节 → 「画像摘要与角色判定」／「效力核验记录」／「分法域义务详述」），**modeB 对比报告无对应映射**——五条批注中四条降级挂文档末段（仅「假设与不确定项」因锚点词「效力核验记录」通用而命中）。**修复** 追加 modeB 五子节映射（概念对齐→「二、概念对齐」／主体对应关系→「四、义务主体对应关系」／假设与不确定项→「九、效力核验记录」／特定规则适用说明→「三、对比矩阵」／行为准则适用与对比维度→「八、行为准则措施要点展开」）；规则按序尝试，先命中的标题词若无对应锚点段落则继续尝试后续规则（不提前放弃）。`_find_anchor` 命中的是**章节标题段落**（标题亦属 `w:p`），为本实现的预期行为。**测试 197→198**（新用例锁定五条批注各自锚定到对应章节标题段落且无降级告警；配反向验证——源码摘除 modeB 规则后用例转红）。**法律口径零改动** |
| ⑨ 2026-09-19 | **效力核验层提示＋B5a×A3 能力×模态校验**（用户当日两项裁定）。**其一（不改脚本）** 法规**全称与简称系同一份文件**：效力核验只做一次、效力表只占一行，不得因正文两种写法重复核验或重复列行——落于阶段3＋§6 ①b、`reporting-rules.md` L6、`validity-checklist.md` 开头；**W-14 的脚本侧归一（简称→全称、补 GB 正则）经用户裁定「停止代码改造」，`check_report.py` 一字未改**（同族已知误报一并留存不修：`LAW_REF_RES` 的 `《…》` 正则只认书名号不认实体，产品名《…》亦会被计入）。**其二（新增代码）** `resolve_triggers.cross_check_capability_modality()`：B5a 勾选的高仿真能力若**所需输出模态在 A3 中完全不存在**（纯文本产品勾「数字人／人脸生成·替换·操控／沉浸式拟真场景／合成人声·仿声／姿态操控」）即判**答案级矛盾（退出码 1）**，须弹窗核对后按「矛盾处理规则」以产品实际描述为准回退；`build_skeleton.py` 同步纳入拒绝生成门（`--force` 放行）。**边界（刻意不代码化）**：反方向的「漏勾」（A3 含模态而 B5a 未勾）不报——风景图／无人脸视频／纯音乐音效可合法不勾，机器无从裁断，仍属人工按「B5a判定备注」五条映射追问；该边界以专门用例 `test_capability_modality_under_tick_is_not_machine_checked` 锁定，防日后被「顺手补上」而制造误报。**测试 190→197**（能力×模态 5 例＋骨架门 2 例，均配反向验证：源码级整块摘除后 `resolve_triggers` 0／`build_skeleton` 0、正例转红）；**语料回归**：案例16-18 审计 JSON 与案例16 业务版重跑均维持 exit 0，零新增误报；**法律口径零改动** |
| ⑧ 2026-09-19 | **md_to_docx 加粗区间吞掉内联标记（脚注引用漏渲染）修复**。触发＝案例16 业务版报告 docx 交付后核对，**正文仅 5 个脚注引用、脚注定义却有 7 条**——`[^eu50_2]`／`[^ca22757_3_2]` 以字面文本残留在 `**(1) …[^eu50_2]**：` 形态的加粗标题里，其定义在 `footnotes.xml` 中沦为**孤儿**（Word 不显示未引用脚注）。**根因** `md_to_docx._tokenize_inline` 把 `**…**` 作为不透明 `InlineToken("bold")` 整体吞下，加粗区间内的 `[^label]`／链接／行内代码从未进入子标记识别；而 `_collect_footnotes` 用的是不受加粗影响的 `_footnote_labels_in_line`，故**定义被收集、引用却从未渲染**（收集与渲染两条路径对加粗的处理不一致＝典型静默失效）。**修复** 加粗分支改为**递归**解析内层文本，文字类子标记统一提升为加粗，`footnote`／`link`／`code` 原样透传。**反向验证**：以旧不透明实现重建临时模块，复现「引用数 0＋字面残留＋定义孤儿」，确认回归测试会转红。新增回归测试 `test_footnote_inside_bold_span_still_renders_a_reference`（加粗标题内脚注×2＋加粗段落内脚注×1 场景）。**附带（同批案例16 运行暴露）** `tests/register_case.py` 增加**幂等守卫**：同一报告路径重复登记由「静默新增重复索引行＋明细节」（原实现仅以「最大序号+1」追加、不去重）改为**拒绝（exit 1）**、`--force` 放行。**测试 187→190**（脚注回归 1 例＋登记守卫正/反/放行 3 例，均配反向验证）。修复后案例16 docx 引用↔定义 **7↔7**、字面残留 0。**法律口径零改动** |
| ⑦ 2026-09-19 | **全脚本代码审查修复**（1 阻断＋3 应修＋7 轻微，依据工作区《…脚本代码审查报告_20260919.md》）。**B-1（阻断）** `lookups.ca_lop_decision` 行为关改**显式三态**：仅字面「是」通过，「否」排除，未采集（`None`）／「不确定」／未识别值一律判**模糊**——原实现把「否」「不确定」特判、其余落空到「存在分发」，致 `distribution=None` 时越过行为关误判「构成」。**S-1** `md_to_docx` 注释剥离由单行扩为**跨行状态机**（`build_skeleton` 两行报头注释不再漏进 docx；代码围栏内保留字面注释）。**S-2** `annotations_to_docx_comments` 批注锚点 `commentRangeStart` 由插首位改为插到 `w:pPr` **之后**（OOXML `CT_P` 要求 pPr 为首子元素）。**S-3** `lint_terms --jurisdictions` 落地（范围外法域节整套跳过；此前为「接收但不生效」死参数）。**轻微**：N-1 删 `check_report` 重复赋值；N-2 删 W-02 的 `in_footnotes` 死变量；N-3 W-22 收窄为**仅正文**（围栏／报头照搬声明块／批注节不计）；N-4 `survey_io` 答案键由裸前缀收紧为 `^[AB]\d`（`Author`／`Batch` 不再误认）；N-5 `forward_nodes_within` docstring 订正（窗口含基准日当天）；N-6 `resolve_triggers` B5b 交互布尔改**显式值域**（防字符串「否」被 `bool()` 判真）；N-7 新增 **W-23**（检查项前置信号未命中致本项未执行）——E-12 模板判定、W-11 范围声明原文匹配不再静默跳过。**测试 166→187**；每批独立同步＋匿名提交；新增/改码一律配正反例＋反向验证；**法律口径零改动** |
| ⑥ 2026-09-18 | **强制门禁静默失效修复（批A~G）**。触发＝4 份实产报告 `check_report`＋`lint_terms` **0E/0W 双绿**却普遍缺人工门禁项（根因＝§6 门禁清单与阶段3/4 硬性要求两张表从未对齐）——**判 skill 缺陷不得以「双绿」为依据**。依据工作区《强制门禁静默失效排查报告_20260918》《…修复执行方案_20260918》《…_批F复核表_20260918》。**批A+B** 新增 E-10（义务详述四要素字段）／E-11（证据等级）／W-14（效力核验记录覆盖率）／W-15（义务详述节缺失，防 E-10/E-11 静默跳过）＋§6 补 ①b①c⑩a⑩b⑩c；**批C** 补「角色间义务关系说明」承载（模板§三【指令】＋三法域节末占位、骨架第三节字段结构占位）；**批D** `cop_digest_verify.py` 纳入生成期交付前必跑（限含欧盟）＋§6 ⑭b＋§7 使用时机；**批E** 新增 `lookups.FORWARD_NODES`＋`forward_nodes_within()`，骨架报头按 `session_date` 出「临近节点提示」独立块（≤60 天）；**批F** §6 余 8 条人工项只读复核表＋加州条号版本锚定修正（`ca-rules` 现行法 hosting (h)→**(g)**、`role-mapping` 补**双锚**、`glossary` 三处误号与非加州法定术语改写、新增「条号锚定」表约定；89 处 `§22757.1(x)` 全仓审计）；**批G** 新增 E-12（modeB 小节存在性）／W-16（确定性推定措辞）／W-17（未覆盖维度与转介卡）／W-18（前瞻块归属）／W-19（批注节回填确认条目）＋ `cop-digest` S2 分组标题命名漂移修正（0E/2W→**0E/0W**）＋骨架净化（生成器自造 E-03/E-04/W-14 清零，骨架态收敛为 E-02/W-17 两个**预期**信号）＋⑤⑦b 前移问卷层自检（`ui-landing-rules.md` §6）。**测试 99→151**；每批独立同步＋匿名提交；新增码一律配正反例＋**反向验证**（移除实现后正例必须转红）。判定规则层仅新增只读查表函数，**法律口径零改动** |
| ⑤ 2026-09-18 | 分流默认反转与呈现层降级（依据工作区《业务版问卷前端-工具落地注意事项》＋《分流默认反转与呈现层降级_执行方案_20260918》）：①起始分流问题改为「偏好精炼／通俗」风格选择（问题·选项·示例三部分整体呈现，示例不得剥离）；②分流未明确时默认②业务版（原默认①），并补答非所问执行规则（重述一次→仍不明则默认并告知可切换）；③SKILL.md 阶段1 skin-aware 化（执行地图资产列、开场照搬、题面锁主句、回显四要素四处呈现锚点）；④法务版加【定位·母版声明】（判定母版＋opt-in 合规框架皮肤）、业务版标默认皮肤；⑤新增 `references/ui-landing-rules.md` 并补索引行与业务版指针。判定规则层零改动 |
| ④ 2026-09-16 | 第三轮盲测（案例19-21）修复批次：modeA-questionnaire B3a 题干口径锁（非累计注册/下载量）＋回填确认量级口径并列回显（V3-02，撤换原「级差≥10倍」启发式）；B3a 档位拒识话术补两档题映射指引（V3-01）；E-07 核验动词（核验/核查/查验/审核/留痕/检查/复核）排除＋分发/传播平台独大场景降级 W-13 复核主体定性＋注入夹具4用例（V3-04）；survey-audit-schema 退出码措辞改「答案级问题：矛盾或无法识别」＋新增题号↔键名映射表＋骨架另读字段补 B1_3 托管平台行（V3-03）；V3-05（B8-1×B4②）经复核裁定不代码化——并存合法态，硬判会诱导改答案过机检；**同日下午文件命名改造**：`assets/` 四个模板加模式前缀（modeA-questionnaire／modeA-report-template／modeB-research-review-template／modeB-comparison-template），全仓 48 处引用同步（SKILL/references/scripts/tests），§7 新增命名约定说明 |
| ③ 2026-09-15 | 代码化落地：新增 `scripts/` 六个工具（check_report 门禁 linter／lint_terms 术语 linter／resolve_triggers 触发引擎／lookups 机械查表／build_skeleton 骨架生成／cop_digest_verify 完整性校验／register_case 案例登记）；§6 门禁逐条括注机器检查码；新增 `references/survey-audit-schema.md` 答案契约；modeA-questionnaire 增「其它夹带量级强制补选档位」输入契约；md_to_docx 链接 scheme 白名单、批注锚定失败改显式告警。**同日修订（案例16-18 矛盾注入轮验证暴露）**：新增 `scripts/survey_io.py` 统一答案读取（修「按文档样例落盘则触发全盘失真」V2-01）；引擎退出码分层 0/1/2；补 B3a×B4_2c 档位交叉校验（V2-05）；术语门禁改为报头声明区／批注节豁免 W 级、缩「标识」判定为术语性用法（V2-03/V2-06）、豁免理由文本不再被扫描（V2-04）、修「GenAI系统」子串误判并按语境分流 AI system 译法（N1/N2/N3）；`utf-8-sig` 兼容 BOM（V2-02）；修 register_case 明细块插入位置（原把索引行号当偏移量，会写进聚合节内部，V2-07） |
| ② 2026-09-14 | 新增 CoP digest 接入：references/cop-digest.md（全量摘要）＋cop-digest-build-rules.md 构建规则；触发 CoP 改为必读 digest、逐字引用按锚点回原文；L1/自检③⑭加 digest 护栏；glossary 收录 9 个行为准则专名 |
| ① 2026-09-11 | 结构重组：调度器化＋执行地图＋法域索引；底线收敛 6 条；细则下沉 reporting-rules.md / modeA-questionnaire.md；自检清单瘦身保留主文件 |
| v1.0 对外基线（2026-09-10） | 初版 |

- **知识截止日**：2026-09-05（规则库 last_verified）。日常小修（改题干、加备注、修措辞、脚本修复、测试）只进 git 提交、不升版本号；仅**对外发布／全量重核法规／重大结构性改动**才升版本号并更新日期（详见上方版本规则）。
- **90天周期复核**：规则库 last_verified 距今超90天、或用户要求复核、或引用超出覆盖、或触及「监控点」→ 启动阶段3联网核验，不得仅依赖 bundled 文本。
- **高波动法规监测清单**（突发生效/修订时先改本节警示并提示用户，再排期全量重核）：
  - 加州 CAITA（SB 942 经 AB 853 修正）：已生效（2026-08-02）。
  - 加州 SB 1000（CAITA 修正案）：已通过议会、待州长签署（超期 2026-09-30 自动成为法律即时生效）；确定性修订见 `references/sb1000-diff.md`；义务基准开关见 `references/ca-rules.md`「〇、效力状态开关」。
  - 欧盟 Digital Omnibus Reg. (EU) 2026/1744：已生效（2026-07-27）；Art. 50 透明度义务未推迟（自 2026-08-02 适用）；高风险义务推迟（Annex III 独立系统至 2027-12-02、嵌入产品至 2028-08-02）。
- **面向读者的时效声明**：本 skill 知识具时效性，高波动法规可能突发生效/修订；读者发现法规已更新时，请在提问时附「按最新法规重新解读」，agent 将优先联网核验官方原文。版本号与知识截止日随每次全量重核更新。
- **免责声明**：定义点在 README；description 仅引用、不重复。
